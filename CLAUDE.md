@AGENTS.md

Claude-specific note:
- Keep this file as a shim. Shared repo instructions belong in `AGENTS.md`.
- Large-file import, local-accelerator, storage, helper-command (`helper:install` / `helper:start` / `helper:check`), helper diagnostics, honest model-cache states, and setup behavior changes should be documented in `AGENTS.md` and `README.md`, not here.
