@AGENTS.md

Claude-specific note:
- Keep this file as a shim. Shared repo instructions belong in `AGENTS.md`.
- Large-file import, microphone recording, live browser dictation, local-accelerator, chunk-worker, storage, backup/import, PWA shortcut, service-worker, helper-command (`helper:install` / `helper:start` / `helper:check`), helper diagnostics, localhost-access/browser-permission behavior, honest model-cache states, setup behavior, and main workflow UI changes should be documented in `AGENTS.md` and `README.md`, not here.
